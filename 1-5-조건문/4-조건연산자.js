let money = 3000;
/// **** 조건 연산자 : 간단한 조건문으로, 두 가지 값 중 하나를 선택할 때 사용**** 
let food = (money >= 3000) ? '김밥' : '굶어';
console.log(`선택된 음식 : ${food}`);


let score = 70;
let isPass = (score >= 60) ? '합격' : '불합격'; // 결과 값이 따로 true:false 안 써도 true, false임!
console.log(`합격여부: ${isPass}`);
